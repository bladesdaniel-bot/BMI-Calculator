# BMI-Calculator
This C++ code uses the Win32 API to build a desktop Body Mass Index (BMI) calculator. It creates a custom GUI with a black background, green labels, red text inputs, and a bright blue output. It computes BMI! :)
